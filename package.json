{
  "name": "typescript-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "type-check": "tsc --noEmit",
    "build": "tsc -b",
    "watch": "tsc -w -p ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.9.2"
  }
}
